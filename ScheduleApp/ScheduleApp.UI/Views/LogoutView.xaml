<UserControl x:Class="ScheduleApp.UI.Views.LogoutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="White">

        <Border Width="620"
                Height="320"
                CornerRadius="40"
                BorderBrush="#B0B0B0"
                BorderThickness="1.5"
                Background="White"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel VerticalAlignment="Center">

                <TextBlock Text="Cerrar sesión"
                           FontSize="34"
                           FontWeight="Bold"
                           Foreground="#1B2A41"
                           HorizontalAlignment="Center"
                           Margin="0,35,0,25"/>

                <TextBlock Text="¿Estas seguro que deseas cerrar la sesión?"
                           FontSize="22"
                           Foreground="#555"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,45"/>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center">
                    
                    <Button Content="Cancelar"
                            Width="180"
                            Height="50"
                            Margin="30"
                            FontSize="20"
                            Background="White"
                            BorderBrush="#A0A0A0"
                            Command="{Binding CancelLogoutCommand}">

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="15"
                                        Padding="10"
                                        SnapsToDevicePixels="True">

                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>

                    </Button>

                    <Button Content="Confirmar"
                            Width="180"
                            Height="50"
                            Margin="30"
                            FontSize="20"
                            Background="#1B2A41"
                            Foreground="White"
                            Command="{Binding ConfirmLogoutCommand}">

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="15"
                                        Padding="10"
                                        SnapsToDevicePixels="True">

                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>

                    </Button>

                </StackPanel>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>