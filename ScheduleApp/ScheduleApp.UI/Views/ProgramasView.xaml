<UserControl x:Class="ScheduleApp.UI.Views.ProgramasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="1100"
             Background="White">

    <Grid Margin="30,20,30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Programas"
                       FontFamily="Segoe UI"
                       FontSize="36"
                       FontWeight="Bold"
                       Foreground="Black"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">

                <Button x:Name="BtnExportar"
                        Width="160" Height="40"
                        Background="#F5F5F5"
                        BorderBrush="#A6A6A6"
                        BorderThickness="1.5"
                        Cursor="Hand"
                        Margin="0,0,10,0"
                        Click="BtnExportar_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="📤" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBlock Text="Exportar" FontSize="15" FontWeight="SemiBold" Foreground="#333333" VerticalAlignment="Center"/>
                                    <TextBlock Text=" ›" FontSize="16" Foreground="#333333" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>

                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="📄  PDF"
                                      FontSize="15"
                                      Command="{Binding ExportPdfCommand}"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>

                <Button Width="40" Height="40"
                        Background="#F5F5F5"
                        BorderBrush="#A6A6A6"
                        BorderThickness="1.5"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <TextBlock Text="⋮" FontSize="20" Foreground="#333333"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </StackPanel>
        </Grid>

        <Border Grid.Row="1"
                Width="450" Height="45"
                BorderBrush="Black"
                BorderThickness="1.5"
                HorizontalAlignment="Left"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="45"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🔍" FontSize="18"
                           VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Gray"/>
                <TextBox Grid.Column="1" BorderThickness="0" FontSize="16"
                         VerticalContentAlignment="Center" Background="Transparent"
                         Text="Buscar programa"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  BorderThickness="1"
                  BorderBrush="#CBD5E1"
                  RowHeight="55"
                  FontSize="14"
                  Margin="0,0,0,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Width="100"/>
                <DataGridTextColumn Header="Nombre" Width="*"/>
                <DataGridTextColumn Header="Jornada" Width="120"/>
                <DataGridTextColumn Header="Nº semestres" Width="130"/>
                <DataGridTemplateColumn Header="Estado" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="#2E7D32" CornerRadius="4" Padding="8,4"
                                    HorizontalAlignment="Center">
                                <TextBlock Text="Activa" Foreground="White"
                                           FontWeight="Bold" HorizontalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Acciones" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="👁" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="4,0"/>
                                <Button Content="✏️" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="4,0"/>
                                <Button Content="🗑️" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="4,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,10,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button Content="≪" FontSize="20" Background="Transparent" BorderThickness="0"
                        Cursor="Hand" Margin="0,0,10,0"/>
                <Border Background="#1B2A41" Width="35" Height="35">
                    <TextBlock Text="1" Foreground="White" FontSize="16"
                               FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Button Content="≫" FontSize="20" Background="Transparent" BorderThickness="0"
                        Cursor="Hand" Margin="10,0,0,0"/>
            </StackPanel>

            <Button HorizontalAlignment="Right"
                    Content="+ Nuevo programa"
                    Width="220" Height="50"
                    Background="#112233" Foreground="White"
                    FontSize="18" FontWeight="Bold" Cursor="Hand" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

    </Grid>
</UserControl>