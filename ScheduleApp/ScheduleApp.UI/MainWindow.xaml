<Window x:Class="ScheduleApp.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ScheduleApp.UI.ViewModels"
        mc:Ignorable="d"
        Title="ScheduleApp"
        Height="600"
        Width="1000">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- MENU -->
        <StackPanel Background="#1B2A41">

            <Button Content="Inicio"
                    Height="50"
                    Margin="10"
                    Command="{Binding ShowDashboardCommand}"/>
            <Button Content="Materias"
                    Height="50"
                    Command="{Binding ShowMateriasCommand}" Click="Button_Click"/>

        </StackPanel>

        <!-- CONTENIDO -->
        <ContentControl Grid.Column="1"
                        Content="{Binding CurrentView}"/>
        <Button Content="Button" HorizontalAlignment="Left" Margin="10,292,0,0" VerticalAlignment="Top" Height="42" Width="177"/>

    </Grid>

</Window>